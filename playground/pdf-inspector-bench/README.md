# pdf-inspector bench

Firecrawl [pdf-inspector](https://github.com/firecrawl/pdf-inspector) vs golden `*.pdftotext.txt` samples.

```bash
# needs Python 3.12 + fastapi/pydantic/pdf-inspector (see apps/extract/requirements.txt)
python3.12 compare.py
```

Output: `last-run.json`
