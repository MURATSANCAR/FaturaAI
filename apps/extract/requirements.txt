fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
python-multipart==0.0.20
# Digital PDF classify + text/markdown (CID-friendly); falls back to pdftotext
pdf-inspector>=0.2.6
# Optional: iPhone HEIC → JPEG (falls back to heif-convert/magick if missing)
pillow-heif>=0.16.0; platform_system != "Windows"
# Photo OCR: PP-OCRv6 Small→Medium (OpenVINO preferred, ONNX fallback)
onnxruntime>=1.17
openvino>=2024.0
opencv-python-headless>=4.8
# GİB QR code reader (needs system libzbar0 — installed by deploy)
pyzbar>=0.1.9
rapidocr>=3.9.0
numpy<2.3
# Layout-agnostic VL (PaddleOCR-VL-1.6). paddlepaddle installed by deploy from CPU index.
# Need >=3.2 for PaddleOCRVL export (3.0.x only has classic PaddleOCR).
paddleocr>=3.2.0
psutil>=5.9
Pillow>=10.0
